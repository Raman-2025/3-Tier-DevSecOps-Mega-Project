name: CI
 
on:
  push:
    branches: [main]

jobs:
  clone:
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: show current directory
        run: pwd

